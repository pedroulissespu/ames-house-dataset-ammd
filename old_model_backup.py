from sklearn.ensemble import RandomForestRegressor
import joblib

# rf_params = {
#     'n_estimators': 200,
#     'max_depth': 30,
#     'min_samples_split': 2
# }

# model = RandomForestRegressor(**rf_params)
# model.fit(X_train, y_train)
# joblib.dump(model, 'old_rf_model.pkl')

# score de treino era ~0.89
